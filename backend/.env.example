# Copy to .env and fill in real values. Never commit .env.

# Supabase — project ref: podlpuvxievstmapnlnk
SUPABASE_URL=https://podlpuvxievstmapnlnk.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...              # legacy service_role JWT from Project Settings → API
SUPABASE_JWT_SECRET=                           # Project Settings → API → JWT Settings

# Stripe (test mode)
STRIPE_SECRET_KEY=sk_test_...                  # full ~107-char secret from Stripe dashboard
STRIPE_WEBHOOK_SECRET=whsec_...                # printed by `stripe listen --forward-to localhost:8000/api/stripe/webhook`

# Gemini (Google AI Studio — free tier at aistudio.google.com)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# App
FRONTEND_URL=http://localhost:3000
ADMIN_EMAIL=kareem.hosny2001@gmail.com
